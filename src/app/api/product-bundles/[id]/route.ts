/**
 * /api/product-bundles/:id — alias for /api/bundles/:id
 * Satisfies the module contract expected by the sprint-50 bundle feature.
 */
export { PATCH, DELETE } from '@/app/api/bundles/[id]/route'
